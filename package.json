{
  "name": "snake",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --open --host",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.50.1",
    "typescript": "^4.6.3",
    "vite": "^2.9.5"
  },
  "dependencies": {
    "minireset.css": "^0.0.7"
  }
}